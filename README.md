# auth-service

Authentication service built with Node.js, Express, and SQLite.

## Scripts

- `npm start` — start the server
- `npm run dev` — start the server in watch mode

## Notes

This project uses a local SQLite database and environment variables for configuration.
